# Server files

This directory currently contains environment construction and dependency
files only. No training runner exists yet.

Add a runner only when its corresponding new-protocol code path and
configuration exist and have passed local tests. Follow
`AUTODL_SERVER_USAGE_GUIDE.md` before any server transfer or execution.

